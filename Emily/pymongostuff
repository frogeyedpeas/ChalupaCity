import pymongo
import sys

from pymongo import MongoClient
connection = MongoClient()
db = connection.rufound



#input_first = raw_input("Enter First Name: ")
first = db.users.find({"firstname": input_first})

#for result_object in first:
		#print result_object

input_last = raw_input("Enter Last Name: ")

last = db.users.find({"lastname": input_last})

db.users.find({})
for result_object in last:
		print result_object
